so basically what I'm thinking is um the file browser is kind of slow when you open a big directory and I think it's because we're like fetching the whole listing every time instead of um caching it or something and also the icons they flicker when you scroll which is probably related you know because we re-render everything so yeah maybe we should look at virtualizing the list and caching the directory listing
