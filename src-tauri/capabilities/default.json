{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window — allows running the bundled process-compose sidecar and opening project URLs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "shell:allow-execute",
    "opener:default",
    "opener:allow-reveal-item-in-dir",
    "dialog:default",
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        { "name": "binaries/process-compose", "sidecar": true, "args": true },
        { "name": "binaries/caddy", "sidecar": true, "args": true },
        { "name": "binaries/mkcert", "sidecar": true, "args": true },
        { "name": "binaries/mailpit", "sidecar": true, "args": true },
        { "name": "binaries/cloudflared", "sidecar": true, "args": true }
      ]
    }
  ]
}
